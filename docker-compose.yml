version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    labels:
      - "com.thelaunchpadtlp.project=cliente-agentico-universal-2050"
      - "com.thelaunchpadtlp.version=1.0.0"
